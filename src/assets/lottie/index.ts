/**
 * Onboarding "boy reading a book" Lottie animation.
 *
 * TO USE A REAL ANIMATION:
 *   1. Get a free "kid / boy reading book" animation from https://lottiefiles.com
 *      (Download → Lottie JSON) and save it here as `boy-reading.json`.
 *   2. Replace the line below with:
 *        export const boyReadingLottie: any = require('./boy-reading.json');
 *
 * Until then this stays `null` and the onboarding falls back to the image (if set)
 * or the vector art — so the app keeps building.
 */
export const boyReadingLottie: any = null;
