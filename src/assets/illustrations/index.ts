import type { ImageSourcePropType } from 'react-native';

/**
 * Onboarding "boy reading a book" illustration.
 *
 * TO USE A REAL IMAGE:
 *   1. Drop your illustration here as `boy-reading.png`
 *      (add `boy-reading@2x.png` / `@3x.png` too for crisp rendering).
 *      Use a transparent-background PNG so it sits on the maroon hero.
 *   2. Replace the line below with:
 *        export const boyReading: ImageSourcePropType | null = require('./boy-reading.png');
 *
 * Until then this stays `null` and the onboarding falls back to the vector art,
 * so the app keeps building.
 */
export const boyReading: ImageSourcePropType | null = null;
